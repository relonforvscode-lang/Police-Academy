# حل مشكلة Discord Rate Limit

## 🔧 التغييرات التي تم إجراؤها

تم تحديث ملف `main/views.py` بالتحسينات التالية:

### 1. **إضافة منطق إعادة المحاولة التلقائية (Automatic Retry Logic)**
- يعيد محاولة الطلب تلقائياً عند الحصول على خطأ 429 (Rate Limited)
- يستخدم **Exponential Backoff**: ينتظر 1 ثانية في المحاولة الأولى، 2 ثانية في الثانية، إلخ
- الحد الأقصى 3 محاولات قبل عرض الخطأ للمستخدم

### 2. **احترام رؤوس Retry-After**
- يقرأ الرأس `Retry-After` من Discord للتقيد بالوقت المطلوب
- يوفر رسالة خطأ أفضل تخبر المستخدم بالانتظار

### 3. **أحكام أفضل للأخطاء**
- اكتشاف تلقائي للأخطاء الشائعة مثل:
  - 401: بيانات اعتماد غير صحيحة
  - 403: مشكلة في الترخيص

## ✅ ما يجب التحقق منه

### 1. **التحقق من بيانات اعتماد Discord**
تأكد من أن لديك في ملف `.env`:

```env
DISCORD_CLIENT_ID=your_actual_client_id
DISCORD_CLIENT_SECRET=your_actual_secret
```

⚠️ **تحذير:** إذا كانت بيانات اعتماداتك خاطئة أو تم نسخها بشكل غير صحيح، سيرسل Discord أخطاء 429 عند كل محاولة.

### 2. **التحقق من إعدادات OAuth في Discord Developer Portal**
اذهب إلى: https://discord.com/developers/applications

- اختر تطبيقك
- اذهب إلى OAuth2 → Redirects
- تأكد من أن لديك redirect URI مثل:
  ```
  https://yourapp.com/apply/discord-callback/
  ```

### 3. **فحص السجلات (Logs)**
تحقق من سجل Django للأخطاء:
```bash
# في terminal Django
python manage.py runserver
# سترى رسائل تفصيلية عن محاولات الاتصال
```

## 🔄 كيفية الاختبار

1. حاول تسجيل الدخول عبر Discord
2. إذا حصلت على خطأ rate limit، سيعيد الجهاز المحاولة تلقائياً
3. إذا استمرت المشكلة، تحقق من:
   - هل DISCORD_CLIENT_ID و DISCORD_CLIENT_SECRET صحيحة؟
   - هل URL في Discord Console صحيح؟
   - هل تقدم Discord نفسها قيودًا على API (تحقق من الحالة: https://discordstatus.com)

## 🚀 إذا استمرت المشكلة

جرّب أحد الحلول التالية:

### الحل 1: زيادة وقت الانتظار بين المحاولات
إذا كنت تتوقع عددًا كبيرًا من المستخدمين:
```python
# غيّر في discord_oauth_login من 30 إلى 60 ثانية
if time_since_attempt < 60:  # بدلاً من 30
```

### الحل 2: استخدام Discord Bot Token بدلاً من OAuth
إذا كنت فقط تريد الوصول إلى معلومات المستخدم، قد تواجه قيودًا أقل.

### الحل 3: التواصل مع Discord Support
إذا كانت المشكلة مستمرة رغم استخدام بيانات اعتماد صحيحة:
- تحقق من حالة Discord API
- اتصل بـ Discord Support

## 📝 ملاحظات إضافية

- التحديث الجديد يحتوي على اختبار أفضل للأخطاء
- رسائل الخطأ الآن أكثر وضوحًا للمستخدم النهائي
- الوقت المتوقع لحل المشكلة: 1-3 ثوانٍ بدلاً من الفشل الفوري
