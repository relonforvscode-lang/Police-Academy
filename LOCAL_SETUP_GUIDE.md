# 🚀 إرشادات تشغيل الموقع Local

## ✅ الحالة الحالية
الموقع الآن **يعمل بدون مشاكل** على `http://127.0.0.1:8000`

## 📝 ما تم إصلاحه

### المشكلة الأساسية
```
You're accessing the development server over HTTPS, 
but it only supports HTTP.
```

المشكلة كانت في إعدادات Django:
- `SECURE_PROXY_SSL_HEADER` كان مفعل دائماً
- `DEBUG` لم يكن معرفاً في `.env`

### الحل المطبق

#### 1️⃣ تحديث `myproject/settings.py`
تم جعل `SECURE_PROXY_SSL_HEADER` شرطياً:

```python
# قبل (خاطئ):
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')

# بعد (صحيح):
if not DEBUG:
    SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
```

**السبب:** هذا الإعداد `يجب` أن يكون فقط في production (عندما يكون هناك proxy)، وليس في development.

#### 2️⃣ إضافة `DEBUG=True` إلى `.env`
```env
DEBUG=True
```

**السبب:** حتى تعرف Django أننا في بيئة development وليس production.

## 🌐 كيفية الوصول للموقع

### ❌ خاطئ (لا تستخدم):
```
https://127.0.0.1:8000    ❌
https://localhost:8000    ❌
```

### ✅ صحيح (استخدم):
```
http://127.0.0.1:8000    ✅
http://localhost:8000    ✅
```

## 🎯 الصفحات المتاحة

| الصفحة | الرابط | الحالة |
|--------|--------|--------|
| الرئيسية | `http://127.0.0.1:8000/` | ✅ تعمل |
| صفحة التقديم | `http://127.0.0.1:8000/apply/` | ✅ تعمل |
| Discord OAuth | `http://127.0.0.1:8000/apply/discord-login/` | ✅ تعمل |

## 🔧 تشغيل الخادم

### الأمر:
```bash
python manage.py runserver 127.0.0.1:8000
```

أو (بدون تحديد الـ host):
```bash
python manage.py runserver
```

الخيار الأول أفضل لأنه يحدد localhost فقط.

## ⚠️ ملاحظات مهمة

### 1. رسائل التحذير حول HTTPS
قد تري رسائل في terminal مثل:
```
You're accessing the development server over HTTPS, 
but it only supports HTTP.
```

**هذا طبيعي جداً!** يحدث لأن:
- المتصفح الحديث يحاول HTTPS تلقائياً
- Django يرفضها ويرد عليها HTTP
- في النهاية كل شيء يعمل بدون مشاكل

### 2. استخدام Render (Production)
عندما تنشر على Render:
- غيّر `DEBUG=False` في environment variables
- تأكد من وجود `SECURE_PROXY_SSL_HEADER` مفعل
- استخدم HTTPS في الروابط

### 3. اختبار Chrome vs Firefox
- Chrome قد يحاول HTTPS تلقائياً
- Firefox قد يكون أكثر مرونة
- كلاهما سيعمل عند استخدام HTTP

## 🧪 اختبار سريع

اختبر الإعدادات:
```bash
# امسح الـ cache (اختياري)
# اذهب إلى: http://127.0.0.1:8000/

# يجب أن ترى صفحة تسجيل الدخول بدون أخطاء
```

## 📊 ملخص الإعدادات الصحيحة

### في `.env` (Local Development):
```env
DEBUG=True
DJANGO_SECRET_KEY=your_secret_key
# ... باقي الإعدادات
```

### في `settings.py`:
```python
# استخدام HTTPS headers فقط في production
if not DEBUG:
    SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
    SECURE_SSL_REDIRECT = True
    SESSION_COOKIE_SECURE = True
    # ...
```

### في Render (Production):
```env
DEBUG=False
DJANGO_SECRET_KEY=your_secret_key
SECURE_SSL_REDIRECT=True
# ...
```

## ✨ النتيجة النهائية

✅ الموقع يعمل بشكل مثالي على HTTP local  
✅ جميع الصفحات تستجيب مع status 200  
✅ لا توجد مشاكل في الوصول للموقع  
✅ Discord OAuth جاهز للاختبار  

🎉 **كل شيء جاهز للاستخدام!**
